## 1. Guard Coverage

- [x] 1.1 Add a failing Node test that scans `openspec/specs` for generated archive-purpose placeholder text.
- [x] 1.2 Add a failing Node test for graceful runtime-audit success shutdown.

## 2. Purpose Cleanup

- [x] 2.1 Replace generated `Purpose` placeholders in archived OpenSpec main specs with concrete capability-specific purpose statements.
- [x] 2.2 Make successful runtime audits use graceful Electron shutdown after report generation.
- [x] 2.3 Verify the placeholder guard and repository validation commands pass.
