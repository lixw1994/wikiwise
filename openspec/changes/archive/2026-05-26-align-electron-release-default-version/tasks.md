## 1. Release Version Guard

- [x] 1.1 Add a failing test proving no-argument release readiness reports use the native app bundle version.

## 2. Release Script

- [x] 2.1 Update `scripts/build-release.sh` to resolve its default version from native app metadata while preserving explicit overrides.
- [x] 2.2 Verify release readiness, packaging, test, build, and OpenSpec validation pass.
