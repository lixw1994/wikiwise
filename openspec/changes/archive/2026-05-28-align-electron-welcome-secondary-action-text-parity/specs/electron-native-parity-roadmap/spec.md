## ADDED Requirements

### Requirement: Welcome Secondary Action Text Phase Tracking

The migration roadmap SHALL record Electron welcome secondary-action text parity as a native shell visual correction phase.

#### Scenario: Welcome secondary action text phase is archived
- **WHEN** the Electron welcome secondary-action text parity change is archived
- **THEN** retained verification records native Swift foreground evidence, Electron CSS override coverage, preserved welcome action behavior, package/build/runtime evidence, and release-readiness blocker evidence
- **AND** remaining final migration evidence still includes actual signed/notarized release execution or an explicitly accepted OpenSpec deviation
