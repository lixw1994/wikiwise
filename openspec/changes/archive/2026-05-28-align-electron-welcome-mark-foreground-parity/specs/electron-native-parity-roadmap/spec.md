## ADDED Requirements

### Requirement: Welcome Mark Foreground Phase Tracking

The migration roadmap SHALL record Electron welcome mark foreground parity as a native shell visual correction phase.

#### Scenario: Welcome mark foreground phase is archived
- **WHEN** the Electron welcome mark foreground parity change is archived
- **THEN** retained verification records native Swift foreground evidence, Electron CSS welcome-mark coverage, preserved welcome screen behavior, package/build/runtime evidence, and release-readiness blocker evidence
- **AND** remaining final migration evidence still includes actual signed/notarized release execution or an explicitly accepted OpenSpec deviation
