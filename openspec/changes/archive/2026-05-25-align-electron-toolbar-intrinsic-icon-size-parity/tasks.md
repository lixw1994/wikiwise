## 1. Test

- [x] 1.1 Add a failing regression test for native intrinsic icon-only toolbar sizing parity.

## 2. Implementation

- [x] 2.1 Update Electron icon-only toolbar CSS so plain controls size to intrinsic symbol/text content without extra fixed boxes or padding.
- [x] 2.2 Run the targeted Electron chrome/menu toolbar test and confirm it passes.

## 3. Verification

- [x] 3.1 Run full repository verification, OpenSpec strict validation, package the Electron macOS app, archive the change, and rerun verification.
