## 1. Test

- [x] 1.1 Add a failing regression test for native FILE/WIKI mode switch enabled-state parity and Wiki fallback behavior.

## 2. Implementation

- [x] 2.1 Update Electron markup and renderer mode rendering so FILE/WIKI controls stay enabled and Wiki mode falls back to source editing when compiled preview is unavailable.
- [x] 2.2 Run the targeted Electron chrome/menu toolbar test and confirm it passes.

## 3. Verification

- [x] 3.1 Run full repository verification, OpenSpec strict validation, package the Electron macOS app, archive the change, and rerun verification.
