## ADDED Requirements

### Requirement: Folder Row Trailing Padding Phase Tracking

The migration roadmap SHALL record Electron folder-row trailing-padding parity as a native sidebar visual correction phase.

#### Scenario: Folder-row trailing-padding phase is archived
- **WHEN** the Electron folder-row trailing-padding parity change is archived
- **THEN** retained verification records native Swift folder/file row padding evidence, Electron CSS row-padding coverage, preserved file-tree row behavior, package/build/runtime evidence, and release-readiness blocker evidence
- **AND** remaining final migration evidence still includes actual signed/notarized release execution or an explicitly accepted OpenSpec deviation
