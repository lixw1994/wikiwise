## ADDED Requirements

### Requirement: Selected File Accent Height Phase Tracking

The migration roadmap SHALL record Electron selected file-tree accent height parity as a native sidebar visual correction phase.

#### Scenario: Selected file accent height phase is archived
- **WHEN** the Electron selected file-tree accent height parity change is archived
- **THEN** retained verification records native Swift selected-row overlay evidence, Electron CSS selected-accent height coverage, preserved file-tree row behavior, package/build/runtime evidence, and release-readiness blocker evidence
- **AND** remaining final migration evidence still includes actual signed/notarized release execution or an explicitly accepted OpenSpec deviation
