## 1. Test Coverage

- [x] 1.1 Add a targeted runtime audit source test requiring watcher runtime evidence markers, synthetic project-change delivery, and watcher failure messages.
- [x] 1.2 Run the targeted runtime audit test and confirm it fails for the missing watcher evidence implementation.

## 2. Runtime Audit Implementation

- [x] 2.1 Track project watcher startup, selected markdown read IPC, and preview compile IPC payloads inside the runtime audit harness.
- [x] 2.2 Emit a controlled `wikiwise:projectChanged` event for selected markdown and CSS refresh semantics during project scenarios.
- [x] 2.3 Persist watcher runtime evidence into DOM/report capture and fail project scenarios when watcher refresh evidence is missing.

## 3. Verification And Archive

- [x] 3.1 Run the targeted runtime audit test and Electron runtime audit command after implementation.
- [x] 3.2 Run full repository validation, OpenSpec validation, and packaging verification.
- [x] 3.3 Archive the OpenSpec change, commit the completed slice, and push the branch.
