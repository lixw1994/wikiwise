{
  "name": "@wikiwise/cloudflare-hub",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/worker.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev --config wrangler.toml",
    "deploy:preflight": "node scripts/deploy-preflight.js",
    "deploy": "wrangler deploy --config wrangler.toml",
    "d1:migrate:local": "wrangler d1 migrations apply wikiwise-hub --local --config wrangler.toml",
    "d1:migrate:remote": "wrangler d1 migrations apply wikiwise-hub --remote --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
